# Best Deck CR - Telegram бот для Clash Royale

Telegram-бот для подбора лучших колод в Clash Royale на основе карт игрока. Бот анализирует карты пользователя и предлагает оптимальные деки с учетом уровня карт и их доступности.

## 🚀 Основные возможности

- Получение информации об игроке по тегу через Clash Royale API
- Поиск популярных деков на основе карт игрока
- Фильтрация деков по уровню карт (исключение эволюций, если карта недоступна)
- Создание визуальных коллажей из карт деки
- Кэширование данных в Redis для оптимизации производительности

## 🛠 Технологии

- NestJS, TypeScript
- Telegraf (Telegram Bot API)
- Cheerio (парсинг HTML)
- Sharp (обработка изображений)
- Redis (кэширование)

## 📦 Установка

### Требования

- Node.js (версия 18 или выше)
- Docker и Docker Compose (для Redis)

### Шаги установки

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd best-deck-cr
```

2. Установите зависимости:

```bash
npm install
```

3. Запустите Redis:

```bash
docker-compose up -d
```

4. Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_telegram_bot_token
CLASH_ROYAL_TOKEN=your_clash_royale_api_token
PORT=3000
```

## ⚙️ Конфигурация

### Переменные окружения

- `BOT_TOKEN` - токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))
- `CLASH_ROYAL_TOKEN` - токен API Clash Royale (получить на [developer.clashroyale.com](https://developer.clashroyale.com))
- `PORT` - порт для запуска приложения (по умолчанию 3000)

## 🏃 Запуск

### Режим разработки

```bash
npm run start:dev
```

### Продакшн режим

```bash
npm run build
npm run start:prod
```

## 💻 Использование

1. Запустите бота в Telegram
2. Отправьте команду `/start` для получения инструкций
3. Отправьте тег вашего аккаунта Clash Royale (например: `#ABC123`)
4. Бот найдет лучшие деки на основе ваших карт и отправит их в виде коллажей

### Команды бота

- `/start` - приветствие и инструкции по использованию
- `/help` - справка по командам
- `/id` - показать ваш chat ID

## 📝 API и сервисы

Бот использует:

- **Clash Royale API** (`proxy.royaleapi.dev`) - для получения информации об игроках. Данные кэшируются в Redis на 10 минут
- **RoyaleAPI** (`royaleapi.com`) - для парсинга популярных деков через веб-скрапинг. HTML-страницы кэшируются на 10 минут

## 📁 Структура проекта

```text
src/
├── app.module.ts              # Главный модуль приложения
├── main.ts                    # Точка входа
├── best-deck/                 # Модуль для работы с деками
│   ├── services/              # Сервисы для получения и парсинга деков
│   ├── helpers/               # Вспомогательные функции
│   └── types/                 # Типы данных
├── bot/                       # Модуль Telegram бота
│   ├── controllers/           # Обработчики команд и сообщений
│   └── services/              # Сервис для создания коллажей
└── common/                    # Общие модули (фильтры, health checks)
```
